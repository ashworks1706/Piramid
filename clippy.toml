# Assertions in tests are supposed to panic. These lints stay hard errors in production code.
allow-panic-in-tests = true
allow-unwrap-in-tests = true
allow-expect-in-tests = true

# Matches `rust-version` in the workspace manifest so clippy flags newer std APIs.
msrv = "1.87"
